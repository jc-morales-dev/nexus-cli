{
  "name": "@nexus/scripts",
  "version": "1.0.0",
  "description": "Scripts package for Nexus",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "bun test",
    "debug-log-subset": "bun run debug-log-subset.ts",
    "typecheck-all": "bun run typecheck-all.ts"
  },
  "sideEffects": false,
  "engines": {
    "bun": "1.3.11"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^1.0.19",
    "@nexus/bigquery": "workspace:*",
    "@nexus/common": "workspace:*"
  },
  "devDependencies": {}
}
